---
import { Button } from "./ui/button";
import { AccountDropdown } from "./AccountDropdown";
import { Feather } from "lucide-react";

const { user } = Astro.locals;
const imageUrl = user ? Astro.locals.pb.files.getUrl(user, user.profile) : "";
---

<header
  class="bg-card text-foreground flex justify-between items-center px-6 py-3 border-b shadow"
>
  <a href="/" class="flex items-center">
    <Feather className="mr-2 text-teal-500" />
    <div class="font-medium">PackLighter</div>
  </a>
  <AccountDropdown user={user} imageUrl={imageUrl} client:load />
</header>
